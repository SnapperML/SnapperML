name: "AutoEncoder Simple"

params:
  train_data: "data/raw/proton_bin_3_4_newconcept.root"
  optimizer: "adam"
  encoding_dim: 4
  epochs: 100
  batch_size: 256

run:
  - src/models/train_simple_autoencoder.py
