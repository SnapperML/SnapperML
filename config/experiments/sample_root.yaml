name: "Sample ROOT input"

input:
  file: "data/raw/proton_bin_3_4_newconcept.root"
  tree: "Tree;1"
  columns:
    - "*fPMTs"
    - "fStations.*"
  label:
  batch_size: 2
  preprocessing: 'my_script.py'

params:
  optimizer: "adam"
  epochs: 100

run:
  - src/models/test_root_input.py
