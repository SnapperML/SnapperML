name: "AutoEncoder Simple"

input:
  file: "data/raw/proton_bin_3_4_newconcept.root"
  tree: "Tree;1"
  columns:
    - "*fPMTs"
    - "fStations.*"
  batch_size: 2

params:
  optimizer: "adam"
  epochs: 100

run:
  - src/models/test_root_input.py
