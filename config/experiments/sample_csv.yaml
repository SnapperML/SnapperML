name: "AutoEncoder Simple"

params:
  input:
    file: "data/raw/file.csv"
    columns:
      - "Muon[xyz]"
      - ["Ev1", "Ev2", "t"]
      - "Px.*"
  optimizer: "adam"
  encoding_dim: 4
  epochs: 100
  batch_size: 256

run:
  - src/models/train_simple_autoencoder.py
